name: sub
version: 1
cabal-version: >= 1.8

library
  exposed-modules:
      Sub.Lib
  default-language: Haskell2010
  other-modules:
      Paths_sub
  hs-source-dirs:
      src
  build-depends:
    base ==4.*
